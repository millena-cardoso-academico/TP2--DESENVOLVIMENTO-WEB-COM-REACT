import React from 'react';
import Alert from '@mui/material/Alert';

const AlertComponent: React.FC = () => {
  return (
    <Alert severity="info">Este é um alerta de informação — confira!</Alert>
  );
};

export default AlertComponent;
