import React from 'react';
import Box from '@mui/material/Box';

const BoxComponent: React.FC = () => {
  return (
    <Box sx={{ width: 300, height: 300, backgroundColor: 'primary.dark' }}>
      Este é um componente Box.
    </Box>
  );
};

export default BoxComponent;
